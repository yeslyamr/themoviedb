import 'package:flutter/material.dart';

abstract class AppColors {
  // static const mainDarkBlue = Color.fromRGBO(3, 37, 63, 1); // themoviedb main color
  // static const secondaryColor = Color.fromRGBO(21, 60, 102, 1);

  static const a = Color.fromRGBO(89, 193, 184, 1);
  static const secondary = Color.fromRGBO(15, 31, 44, 1);
  static const main = Color.fromRGBO(12, 24, 36, 1);

  static const mainText = Color.fromRGBO(229, 247, 239, 1);
  static const secondaryText = Color.fromRGBO(82, 103, 119, 1);
}
